#include "main.h"

/**
 * binary_to_uint - converts a binary number to an
 *
 * unsigned int.
 * @b: binary.
 * Return: unsigned int.
 * */
unsigned int binary_to_uint(const char *b);
{
	 unsigned int n=0;
	 if (*b == '0')
	 {
	printf("%u\n",*b);
	 }
	else if (*b == '1')
	return (n);	
	{
	printf("%u\n",*b);
	return (n);
	}
	else
	{
	return (n);
	}
	while(*b == "0" && *b == "1")
	{
	printf("*b is null%u,*b==0);
	return (n);
	}
